<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="main_rb_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="first_rb_style">
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColor">@color/selector_main_rb_text_color</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="first_rbline_style">
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:button">@null</item>
        <item name="android:background">#444</item>
        <item name="android:enabled">false</item>
        <item name="android:layout_width">1px</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <declare-styleable name="DRadioButton">
        <attr name="num" format="integer" />
        <attr name="showDot" format="boolean" />
        <attr name="numColor" format="color|reference|integer" />
    </declare-styleable>
    <style name="pop">
        <item name="android:windowEnterAnimation">@anim/fragment_trans_in</item>
        <item name="android:windowExitAnimation">@anim/fragment_trans_out</item>
    </style>

    <style name="style_info_tab">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="style_dialog_addcart_choose_rb">
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@drawable/selector_add_size_rb_bg</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">5dp</item>
    </style>

    <style name="style_dialog_addcart_choose_rg">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="style_add_subtract_btn">

        <item name="android:layout_width">25dp</item>
        <item name="android:layout_height">25dp</item>
        <item name="android:layout_marginLeft">30dp</item>
        <item name="android:background">@drawable/dialog_add_subtract_btn_bg</item>
        <item name="android:textColor">#fff</item>
    </style>

    <declare-styleable name="DTabView">
        <attr name="textSize" format="dimension" />
        <attr name="textColor" format="color|reference" />
        <attr name="text" format="string|reference" />
        <attr name="draw" format="reference" />
    </declare-styleable>

    <style name="style_dialog_add">
        <item name="android:windowEnterAnimation">@anim/dialog_add_cart_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_add_cart_out</item>
    </style>

    <style name="toolbar_group">
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">#323232</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
    </style>

    <style name="style_cart_check_cb">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:button">@null</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="style_toolbar_menu">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:background">@null</item>
    </style>

    <style name="style_input_edt">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:singleLine">true</item>
    </style>
    <!--<sty-->
</resources>
